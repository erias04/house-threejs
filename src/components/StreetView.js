import { Sphere, useCubeTexture, CubeCamera, PerspectiveCamera } from '@react-three/drei'
import * as THREE from 'three'
import { TextureLoader } from 'three'
import { extend, useLoader } from '@react-three/fiber'

